(ns wombats-web-client.constants.local-storage)

;; Users unique authentication token
(def token "token")
(def access-token "access-token")

;; Used for new user enrollment
(def access-key "access-key")
