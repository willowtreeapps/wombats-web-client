(ns battle_bots_web_client.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [battle_bots_web_client.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
